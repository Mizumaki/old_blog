/*
import { connect } from 'react-redux';
import { RouteComponentProps, withRouter } from 'react-router-dom';
import HandlePagesComponent from '../components/HandlePages';
import { IAppState } from '../../states/ducks/index';

const mapStateToProps = (_: IAppState, ownProps: RouteComponentProps) => {
  return Object.assign({}, ownProps)
}

const HandlePages = connect(
  mapStateToProps
)(HandlePagesComponent)

export default withRouter(HandlePages);
*/